# AFIP Phase U Pack 3.4.8 Hotfix 1

Direct-overlay package. Copy every visible file and folder from the ZIP directly into `C:\AFIP` and replace existing files.

Run:

```powershell
cd C:\AFIP
Set-ExecutionPolicy -Scope Process Bypass
.\RUN_PHASE_U_PACK_3_4_8_COMMAND_CENTER.ps1
```
