Runtime changes required:
- Accept legacy capital fields as compatibility input only.
- Never use legacy fields for lot calculation.
- Capital Authority remains final authority.

Ownership markers:
binding_changed_before_order_send
mt5_terminal_path_mismatch
