# Milestone M Pack 8 — Historical Pattern Database

Adds a deterministic, immutable, deduplicated historical Pattern database for research knowledge. It stores versioned Pattern and Cluster snapshots with chronology, checksums, source lineage, validation lineage, explainability lineage, Market Regime indexes, and scope indexes.

Production Knowledge Approval remains disabled. Execution remains `LOCKED_SIMULATION_ONLY`, Direct Execution remains false, Live Execution remains disabled, and no order is sent.
