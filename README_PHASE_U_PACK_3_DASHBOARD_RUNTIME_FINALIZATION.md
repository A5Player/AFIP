# Phase U Pack 3 — Dashboard Runtime Finalization

Patch-only dashboard correction. It creates a three-page landing page, prevents synchronous automatic research during normal dashboard generation, and verifies clean CLI termination. Trading and execution authority are unchanged.

Extract directly into the AFIP repository root and run the INSTALL and RUN PowerShell scripts.
