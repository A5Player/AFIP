# Technical Guide

`DataQualityScorer.assess()` accepts normalized dimension scores plus integrity,
certification, and catalog status. Weights must total 1.0. Classification is deterministic.
Timestamps are observational; assessment IDs are content-derived.
