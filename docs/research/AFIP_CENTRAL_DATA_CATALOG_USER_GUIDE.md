# User Guide

1. Give every dataset a stable `dataset_id`.
2. Keep `dataset_info.json` beside the dataset.
3. Store all research datasets under `data/`.
4. Register datasets explicitly after review.
5. Generate snapshots for traceability.
6. Never delete historical catalog snapshots.
