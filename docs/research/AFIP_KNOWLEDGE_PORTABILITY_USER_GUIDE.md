# User Guide

Use the engine to export a selected AFIP dataset, verify the resulting ZIP, and import it into an isolated recovery directory. Always run verify-only first. Keep the original bundle and manifest together. Never copy recovered files over active data until a human review confirms schema compatibility and lineage. See `AFIP_KNOWLEDGE_PORTABILITY_RECOVERY_RUNBOOK.md` for the operational sequence.
