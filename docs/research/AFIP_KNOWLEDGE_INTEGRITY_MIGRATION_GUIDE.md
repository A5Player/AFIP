# Migration Guide

Schema 1.0.0 is additive. Future schema migrations must preserve original reports and ledgers.
Never rewrite historical JSONL audit entries. Create a new schema version and document field mappings.
