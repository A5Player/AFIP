# Market Regime V2 Developer Guide
`MarketRegimeV2Runtime` accepts normalized component status/effect/score/confidence fields. It uses weighted deterministic aggregation, minimum readiness gates, Version 1 broker/symbol policy, and always returns `execution_allowed=False`.
