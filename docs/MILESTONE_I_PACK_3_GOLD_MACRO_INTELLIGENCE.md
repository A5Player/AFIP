# Milestone I Pack 3 — Gold Macro Intelligence

Deterministic, read-only macro intelligence for XM `GOLD#`. It classifies CPI, PPI, employment, GDP, PMI, and ISM observations, compares actual values with forecast or previous values, and produces structured gold context. Macro data cannot execute orders directly. Live execution remains disabled.
